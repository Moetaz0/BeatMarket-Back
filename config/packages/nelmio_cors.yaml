nelmio_cors:
    defaults:
        allow_credentials: false
        allow_origin: ["*"] # Allow all origins, or specify your front-end URL
        allow_headers: ["Content-Type", "Authorization"]
        allow_methods: ["POST", "GET", "PUT", "DELETE"]
        expose_headers: []
        max_age: 3600
